import { PodswietlanieDirective } from './podswietlanie.directive';

describe('PodswietlanieDirective', () => {
  it('should create an instance', () => {
    const directive = new PodswietlanieDirective();
    expect(directive).toBeTruthy();
  });
});
